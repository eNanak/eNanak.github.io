document.addEventListener('DOMContentLoaded', () => {
    // Inicialización de AOS para animaciones
    AOS.init({ duration: 800, once: true });
    
    // Puedes añadir aquí cualquier script específico para la página Blog
    console.log('Página Blog cargada');
    
    // Ejemplo: Podrías añadir aquí la lógica para filtrar artículos por categorías
    // o cargar más artículos cuando el usuario llegue al final de la página
});